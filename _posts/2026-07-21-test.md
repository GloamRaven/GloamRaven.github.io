---
title: Nord Theme Test
date: 2026-07-21 12:00:00 +0900
categories: [Test]
tags: [test]
---

Check inline code: `sudo nmap -sV target` 이렇게 나와.

## Python

```python
import requests

def fetch(url: str) -> int:
    # comment: send a request
    resp = requests.get(url, timeout=5)
    return resp.status_code   # 200
```

## Bash

```bash
#!/bin/bash
for host in $(cat hosts.txt); do
    echo "scanning $host"
    nmap -p 80,443 "$host"
done
```

## Diff

```diff
- password = "admin123"
+ password = os.environ["APP_PASSWORD"]
```